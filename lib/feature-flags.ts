// Keep temporary product switches centralized so every entry point agrees.
export const COURSE_ROSTER_ENABLED = false;

// Deprecated for now; keep the implementation available for a future re-enable.
export const SYLLABUS_IMPORT_ENABLED = false;
